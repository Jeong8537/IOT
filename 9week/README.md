Raspberry pi
===

Raspberry pi 설치

### IP 설정
Edit Connections -> Ethernet -> Setting -> 
ipv4 Settings -> Manual(Method) -> Add(bash)

```bash
Address = ipv4 Address
Netmask = XX
GateWay = Gateway
```

